export default function CompetencyRadar({ competencies = [] }) { const items = competencies.length ? competencies : ['Communication', 'Critical Thinking', 'Creativity', 'Citizenship', 'Digital Literacy', 'Learning to Learn', 'Self-Efficacy'].map((label) => ({ label, score: 70 })); return <div className="grid gap-3">{items.map((item) => <div key={item.label || item.competency}><div className="flex justify-between text-sm font-semibold"><span>{item.label}</span><span>{item.score}%</span></div><div className="h-3 rounded-full bg-slate-100"><div className="h-3 rounded-full bg-gradient-to-r from-stem-cyan to-social-gold" style={{ width: `${item.score}%` }}/></div></div>)}</div>; }
