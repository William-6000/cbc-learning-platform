import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext.jsx';
export default function Login() { const { login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ email: 'admin@cbc.ke', password: 'Password123!', role: 'ADMIN' }); const [error, setError] = useState(''); async function submit(e){ e.preventDefault(); try { const user = await login(form); navigate(user.role === 'ADMIN' ? '/admin' : `/${user.role.toLowerCase()}/dashboard`); } catch(err){ setError(err.message); } } return <section className="mx-auto max-w-md card"><h1 className="font-display text-3xl font-black">Login</h1><p className="mb-4 text-slate-600">Access your CBC workspace.</p>{error && <p className="rounded-xl bg-red-50 p-3 text-red-700">{error}</p>}<form onSubmit={submit} className="grid gap-3"><input className="input" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><select className="input" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{['STUDENT','TEACHER','PARENT','ADMIN'].map(r=><option key={r}>{r}</option>)}</select><button className="btn bg-stem-navy text-white">Login</button></form><Link className="mt-4 block text-sm font-bold" to="/forgot-password">Forgot password?</Link></section>; }
