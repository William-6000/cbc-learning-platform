export default function ProgressRing({ value = 0, label = 'Progress' }) { const deg = Math.round(value * 3.6); return <div className="flex items-center gap-4"><div className="grid h-24 w-24 place-items-center rounded-full" style={{ background: `conic-gradient(#00C2CB ${deg}deg,#e5e7eb 0)` }}><div className="grid h-16 w-16 place-items-center rounded-full bg-white font-display font-black">{value}%</div></div><div><p className="font-bold">{label}</p><p className="text-sm text-slate-500">Weekly lessons completed</p></div></div>; }
