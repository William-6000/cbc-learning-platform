import { usePathways } from '../context/PathwayContext.jsx';
const themes = { STEM:'from-stem-navy to-stem-cyan', SOCIAL_SCIENCES:'from-social-green to-social-gold', ARTS_AND_SPORTS:'from-arts-burgundy to-arts-orange' };
export default function Pathways(){ const pathways = usePathways(); return <div className="grid gap-8"><section className="rounded-[2rem] bg-white p-8"><h1 className="font-display text-4xl font-black">Choose your Senior School pathway</h1><p className="mt-2 max-w-3xl text-slate-600">Explore KICD-aligned Grade 10–12 pathways, careers, KUCCPS courses and TVET options before selecting subjects.</p></section><div className="grid gap-5 md:grid-cols-3">{pathways.map(p=><article key={p.code} className={`rounded-[2rem] bg-gradient-to-br ${themes[p.code]} p-1`}><div className="h-full rounded-[1.8rem] bg-white/95 p-5"><h2 className="font-display text-2xl font-black">{p.name}</h2><p className="mt-2 text-sm">{p.description}</p><h3 className="mt-4 font-bold">Subjects</h3><p className="text-sm text-slate-600">{[...new Set(p.subjects?.map(s=>s.name))].slice(0,8).join(', ')}</p><h3 className="mt-4 font-bold">Career outcomes</h3><ul className="list-disc pl-5 text-sm">{p.careerOutcomes?.map(c=><li key={c}>{c}</li>)}</ul></div></article>)}</div><section className="card overflow-x-auto"><h2 className="font-display text-2xl font-black">Pathway comparison</h2><table className="mt-4 w-full text-left text-sm"><thead><tr><th>Pathway</th><th>KUCCPS examples</th><th>TVET options</th></tr></thead><tbody>{pathways.map(p=><tr className="border-t" key={p.code}><td className="py-3 font-bold">{p.name}</td><td>{p.kuccpsCourses?.join(', ')}</td><td>{p.tvetOptions?.join(', ')}</td></tr>)}</tbody></table></section></div>; }
