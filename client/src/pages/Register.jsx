import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext.jsx'; import { useApi } from '../hooks/useApi.js';
export default function Register(){ const { register } = useAuth(); const navigate = useNavigate(); const { data: schools=[] } = useApi('/api/schools', []); const [form,setForm]=useState({ name:'', email:'', password:'', role:'STUDENT', pathway:'STEM', grade:10, schoolId:'' }); async function submit(e){ e.preventDefault(); const user = await register({ ...form, schoolId: form.schoolId || schools[0]?.id }); navigate(user.role === 'STUDENT' ? '/student/dashboard' : '/login'); } return <section className="mx-auto max-w-2xl card"><h1 className="font-display text-3xl font-black">Register learner or staff</h1><form onSubmit={submit} className="mt-4 grid gap-3 md:grid-cols-2"><input className="input" placeholder="Full name" onChange={e=>setForm({...form,name:e.target.value})}/><input className="input" placeholder="Email" onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder="Password" onChange={e=>setForm({...form,password:e.target.value})}/><select className="input" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{['STUDENT','TEACHER','PARENT','ADMIN'].map(r=><option key={r}>{r}</option>)}</select><select className="input" value={form.pathway} onChange={e=>setForm({...form,pathway:e.target.value})}>{['STEM','SOCIAL_SCIENCES','ARTS_AND_SPORTS'].map(p=><option key={p}>{p}</option>)}</select><select className="input" value={form.grade} onChange={e=>setForm({...form,grade:e.target.value})}>{[10,11,12].map(g=><option key={g}>{g}</option>)}</select><select className="input md:col-span-2" onChange={e=>setForm({...form,schoolId:e.target.value})}>{schools.map(s=><option value={s.id} key={s.id}>{s.name} — {s.county}</option>)}</select><button className="btn bg-social-green text-white md:col-span-2">Create account</button></form></section>; }
